@model IEnumerable<PersonSearchHC.Models.Person>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Add New", "Create")


<p>
    Name: <input type="text" id="nameSearch" /><br />
</p>

</p>
@*<table class="table">
        <tr>
            <th>
                First Name
            </th>
            <th>
                Last Name
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.FirstName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.LastName)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ID })
            </td>
        </tr>
    }

    </table>*@
<div id="div1">
    @{Html.RenderPartial("_ShowNames");}
    @*@Html.Action("ShowNames", "People")*@
</div>


@section Scripts{
    <script type="text/javascript" src="~/Scripts/jquery-1.10.2.js"></script>
    <script type="text/javascript" src="~/Scripts/jquery.unobtrusive-ajax.js"></script>
    <script type="text/javascript">
    $(document).ready(function () {
        $("#nameSearch").on('input propertychange paste', function () {
            var name = $("#nameSearch").val();
               $("#div1").load('@(Url.Action("ShowNames","People",null, Request.Url.Scheme))?name=' + name);
        });
    });
    </script>
}
